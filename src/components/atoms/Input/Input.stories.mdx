import { Meta } from '@storybook/addon-docs'

import { Canvas } from 'core/storybookWrappers'

import { Input } from './Input'

<Meta title='Components/Input' />

# Input

This is a simple HTML `input` element with xstyled super powers.

**Important:** Do not use this component in place of the TextField, as this is
a raw input. Use it only if you know what you're doing and need pure input element with xstyled props.

## Usage

<Canvas>
  <Input />
</Canvas>
